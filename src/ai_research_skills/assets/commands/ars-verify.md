---
disable-model-invocation: true
---
# /ars-verify

Run the explicitly user-requested `ars-verify` citation, provenance, number, and source-consistency
review. This is a thin alias for the standalone skill; it does not invoke `/ars-audit` or any
other skill automatically, and it does not create or repair a workspace unless requested.
