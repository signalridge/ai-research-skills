# Screening card — optional

When the user asks to screen sources, define relevance against the question and explain each
selection briefly. A score can make later threshold changes easier, but it is optional.

Keep `include`, `exclude`, and `unsure` distinct when those fields exist in the workspace. Do
not force an unresolved source into a binary decision. Preserve the query/source provenance
and report how many candidates were not inspected.
