# ILLUSTRATIVE EXAMPLE — the five records below are synthetic.  The example keeps a
# contrarian disagreement, an unsure record, and avoided/unexplored cells so the teaching
# signals remain visible while every ledger is self-consistent.
topic: retrieval-augmented-agents
question: "Do retrieval-augmented agents outperform long-context models on multi-hop QA when retrieval recall is held constant?"
created: 2026-07-20
last_searched_at: 2026-08-03
phase: 5
scope:
  in: [multi-hop QA, agentic retrieval, long-context baselines]
  out: [single-hop QA, RAG for code, multimodal retrieval]
  window: 2023-01-01..
  venues: [NeurIPS, ICLR, ICML, ACL, EMNLP, COLM]
axes:
  - name: method
    values: [iterative retrieval, long-context]
  - name: control
    values: [none, fixed token budget, fixed retrieval recall]
  - name: evaluation
    values: [2-hop, 3+-hop]
recall_modes:
  keyword:
    - {tool: arxiv.search_papers, query: 'ti:"multi-hop" AND abs:"retrieval augmented"', categories: [cs.CL], max_results: 50}
  citation_chain:
    - {tool: openalex.get_citation_graph, seed_id: W2626778328, direction: cites, per_page: 100}
  venue_author:
    - {tool: openalex.search_entities, entity_type: works, query: retrieval, filters: {publication_year: "2025-2026"}, per_page: 100}
  contrarian:
    - {tool: arxiv.search_papers, query: 'abs:"long context is sufficient"', categories: [cs.CL], max_results: 50}
screen:
  include: ["evaluates >=2-hop", "reports a retrieval-quality ablation"]
  exclude: ["survey papers", "no empirical evaluation", "single-hop only"]
  relevance_threshold: 6
counts:
  retrieved: 5
  deduped: 5
  adjudicated: 4
  scored_at_or_above_threshold: 4
  unsure: 1
  fulltext_kept: 3
saturation:
  rounds: 3
  new_on_topic_last_round: 0
  baseline_growth: "illustrative stable snapshot"
  stop_rule: "no new on-topic synthetic record in the final round"
